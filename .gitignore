# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage
/test-results/
/playwright-report/
/playwright/.cache/
/blob-report/

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local

# OCR transcription cache — full third-party manual text + some internal business
# docs; kept LOCAL only (this repo is public). The text lives in the RLS-locked DB
# after ingest (same posture as the rest of the KB pool); cache just avoids re-OCR.
/scripts/ocr-cache/

# throwaway dev/verification harnesses (scripts/_*.mjs)
/scripts/_*.mjs

# claude code local config / session data
.claude/

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

.vercel
